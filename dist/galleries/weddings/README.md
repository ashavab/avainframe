Drop wedding shoot photos into this folder.

Files served statically from `/galleries/weddings/`.

Suggested naming: `YYYY-MM-DD-description.jpg`

After adding images, run:

```bash
npm run galleries:generate
```

This will regenerate `src/app/data/portfolioGenerated.ts` used by the site.